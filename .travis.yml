language: scala
scala:
  - 2.11.12
  - 2.12.7
jdk:
  - oraclejdk8
sudo: false
script:
- sbt ++$TRAVIS_SCALA_VERSION test unidoc
- git diff --exit-code
matrix:
  include:
  - jdk: openjdk11
    scala: 2.12.7
    script:
    - sbt ++$TRAVIS_SCALA_VERSION test
addons:
  apt:
    packages:
      - oracle-java8-installer
